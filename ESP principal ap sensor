#CONFIGURACION COMO ACCESSPOINT# ESP32_PRINCIPAL_AP_SENSOR.py 
import network
import socket
import time

# Configuración del Access Point
SSID -"Sebas"
PASSWORD = "1234sebas" # Minimo 8 caracteres

ap = network.WLAN(network.AP_IF)
ap.config(essid-SSID, password=PASSWORD, authmode=network.AUTH_WPA_WPA2_PSK)
ap.active(True)

# IP fija para el AP
ap.ifconfig(('192.168.4.1°,'255.255.255.0','192.168.4.1','8.8.8.8'))
print("AP activo")
print("SSID:",SSID)
print("Contraseña:",PASSWORD)
printi"IP del AP:", ap.ifconfig()[e])
print("Conecta tu teléfono a esta red para usar Fing. \n")

# Servidor TCP para recibir datos del sensor
def start server():